# config file for "simil" file comparison
[config]
# statements to ignore
ignore = [
    "",  # ignore empty lines
    # Python
    "else:",
    "\"\"\"",
    "\"\"\")",
    "'''",
    "(", ")",
    "{", "}",
    "[", "]",
    "})",
    "continue",
    "break",
    "return",
    "print()",
    "print(",
    "print(\"\"\"",
]
# ignore lines beginning with ...
ignore_beginning = [
    # Python
    "import",
    "from",
    # Rust
    # Rust
    "use",
    "/*", "*/",
    "exit",
    "} else {"
]
trim_whitespace = true